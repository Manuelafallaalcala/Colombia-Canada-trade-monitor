# MIC-CC — dependencias del modelo financiero de la Fase 4.
#
# Solo dependencias DIRECTAS, con la version usada para producir los resultados
# publicados. pip resuelve las transitivas.
#
#   python -m venv .venv
#   .venv\Scripts\activate          # Windows  (source .venv/bin/activate en macOS/Linux)
#   pip install -r requirements.txt
#
# La version anterior de este archivo era un `pip freeze` completo (69 lineas) e
# incluia `nest-asyncio2`, que la auditoria marco para verificar antes de publicar
# el repositorio. Verificado: es un fork mantenido de `nest-asyncio` que instala
# `ipykernel` 7.x como dependencia transitiva, no un paquete anadido por error. Por
# ser transitiva no corresponde declararla aqui.

# --- nucleo del modelo -----------------------------------------------------
numpy==2.5.1
pandas==3.0.3
requests==2.34.2          # descarga en vivo de TRM (BanRep) y USD/CAD (Bank of Canada)
openpyxl==3.1.5           # lectura y escritura de los .xlsx de datos y resultados

# --- figuras ---------------------------------------------------------------
matplotlib==3.11.1

# --- ejecucion del cuaderno ------------------------------------------------
ipykernel==7.3.0
nbconvert==7.17.1
nbformat==5.10.4
nbclient==0.11.0

# --- pruebas ---------------------------------------------------------------
pytest==9.1.1
